{% extends 'base.html.twig' %}

{% block title %}MessagePri{% endblock %}

{% block body %}
    <h1>MessagePri</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>IdMessage</th>
                <td>{{ message_pri.idMessage }}</td>
            </tr>
            <tr>
                <th>Message</th>
                <td>{{ message_pri.message }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('message_pri_index') }}">back to list</a>

    <a href="{{ path('message_pri_edit', {'idMessage': message_pri.idMessage}) }}">edit</a>

    {{ include('message_pri/_delete_form.html.twig') }}
{% endblock %}
