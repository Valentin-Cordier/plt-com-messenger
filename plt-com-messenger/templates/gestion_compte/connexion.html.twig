{% extends 'plt_com_messenger/indexnav.html.twig' %}
{% block title %}Connexion{% endblock %}
{% block body %}
    <h1>Connexion !</h1>

    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey }}</div>
    {% endif %} 
            <h2>Formulaire de connexion ! ✅</h2>
                <form action="{{ path('connexion') }}" method="POST">
                    <div class="form-group">
                        <input placeholder="Username ..." name="_username" type="text" class="form-control" value="{{ last_username }}">
                    </div>
                    <div class="form-group">
                        <input placeholder="Mot de passe ..." name="_password" type="password" class="form-control">
                    </div>
                    <div class="form-group">
                    <button type="submit" class="btn btn-success">Connexion !</button>

                    </div>
                </form>
{% endblock %}